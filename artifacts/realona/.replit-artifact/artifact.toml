kind = "web"
previewPath = "/"
title = "Realona Exchange"
version = "1.0.0"
id = "artifacts/realona"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22481

[services.development]
run = "pnpm --filter @workspace/realona run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/realona", "run", "build" ]
publicDir = "artifacts/realona/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22481"
BASE_PATH = "/"
